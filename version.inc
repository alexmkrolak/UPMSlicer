# Included by CMakeLists, edited by the build script
# (the version numbers are generated by the build script from the git current label)

set(SLIC3R_APP_NAME "Mosso Slicer")
set(SLIC3R_APP_KEY "MossoSlicer")
# Keep using the upstream gettext catalogs while storing Mosso Slicer settings
# under a separate application identity.
set(SLIC3R_TRANSLATION_CATALOG "OrcaSlicer")
# Mosso Slicer is deliberately a local-only slicer. Printer-host communication
# such as OctoPrint remains available, but account, cloud storage, cloud preset,
# and cloud plugin services are not created or exposed.
set(MOSSO_SLICER_LOCAL_ONLY "1")

if(NOT DEFINED BBL_INTERNAL_TESTING)
set(BBL_INTERNAL_TESTING "0")
endif()
set(SoftFever_VERSION "2.5.0-dev")
string(REGEX MATCH "^([0-9]+)\\.([0-9]+)\\.([0-9]+)"
       SoftFever_VERSION_MATCH ${SoftFever_VERSION})
set(ORCA_VERSION_MAJOR ${CMAKE_MATCH_1})
set(ORCA_VERSION_MINOR ${CMAKE_MATCH_2})
set(ORCA_VERSION_PATCH ${CMAKE_MATCH_3})

set(SLIC3R_VERSION "02.08.01.55")
string(REPLACE "." "," SLIC3R_RC_VERSION "${SLIC3R_VERSION}")

if (NOT DEFINED ORCA_UPDATER_SIG_KEY)
    set(ORCA_UPDATER_SIG_KEY "" CACHE STRING "Base64url encoded updater signature key")
endif()
